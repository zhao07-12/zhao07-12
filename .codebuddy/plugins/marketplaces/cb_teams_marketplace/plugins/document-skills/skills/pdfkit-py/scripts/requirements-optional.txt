# pdfkit lite - optional dependencies
# Install all manually: pip install -r requirements-optional.txt

# Table extraction (extract_table) - about 50MB incl. opencv
camelot-py[cv]>=0.11.0
tabula-py>=2.8.0

# PDF to Word (pdf_to_word)
pdf2docx>=0.5.6

# PDF compression optimization (compress)
pikepdf>=8.0.0

# Formula detection model mode (formula_detect) - about 200MB+ incl. PyTorch stack
pix2tex>=0.1.1
